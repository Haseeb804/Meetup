import 'package:flutter/material.dart';

class AppColors {
  // A soft pink for the main background
  static const Color scaffoldBackground = Color(0xFFFCEAE4);

  // A slightly darker pink or beige for shapes or highlights (optional)
  static const Color shapeColor = Color(0xFFEBD5CA);

  // Warm brown for buttons, text accents
  static const Color brown = Color(0xFF5E3C23);

  // Text color if you want a slightly lighter or darker brown
  static const Color textColor = Color(0xFF4A2D19);
}
